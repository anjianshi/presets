{
  "compilerOptions": {
    // 允许用 '@/xxx' 的路径引入文件。
    // 若是用 tsc 来构建，必须引入 ts-alias，否则只能保证类型不出错，但构建结果会保留 `@/xxx`（甚至在类型定义里会把原本没用 @ 引用的路径改为 @ 引用）。
    "baseUrl": "../../../src",
    "paths": {
      "@/*": ["./*"]
    }
  }
}
