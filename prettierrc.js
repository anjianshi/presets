// 只定义与默认值不同的项，完整列表见：
// Only define items that differ from the default value. The full list:
// https://prettier.io/docs/en/options.html
module.exports = {
  printWidth: 100,
  semi: false,
  singleQuote: true,
  arrowParens: 'avoid',
}
